Lab2 - Cuckoo Hashing on GPUs with CUDA
Guanzhou HU

* The following prerequisites should be satisfied:
  1. CUDA toolkit >= 10.0
  2. C++ library >= C++11 standard
  3. g++ / clang++
  4. CUDA driver & workable GPU cards

* To compile the code, do: `make`

* To run the demo, do: `./demo`

* To run the performance test, do `./perf-test`

* To clean the build, do: `make clean`
